#include <iostream>
#include <pthread.h>
#include <vector>
using namespace std;

int main(){
    //默认初始化
    vector<int> v1;
    //列表初始化
    // vector<char>v2 = {'a','b','c'};
    // vector<char>v3 = {'a','b','c'};
    return 0;
}
